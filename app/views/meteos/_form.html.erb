<%= form_for(@meteo) do |f| %>
  <% if @meteo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@meteo.errors.count, "error") %> prohibited this meteo from being saved:</h2>

      <ul>
      <% @meteo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :city %><br>
    <%= f.text_field :city %>
  </div>
  <div class="field">
    <%= f.label :temperature %><br>
    <%= f.number_field :temperature %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
